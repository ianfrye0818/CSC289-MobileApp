services:
  db:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: mssql-server
    restart: unless-stopped
    environment:
      - SA_PASSWORD='CSC289@2026!'
      - ACCEPT_EULA=Y
    ports:
      - 1433:1433
    volumes:
      - db_data:/var/opt/mssql

volumes:
  db_data:
